public SkyView (int numRows, int numCols, double[] scanned){
	view = new double [numRows][numCols];
	int viewPos = 0;
	int r = 0, c = 0;
	do{
		if (r % 2 == 1)
			for ()
	}while (viewPos < scanned.length)
}